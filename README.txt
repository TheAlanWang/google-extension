# Built‑in AI Minimal Test (MV3, CSP Clean)

1) Go to chrome://extensions → Enable Developer mode → "Load unpacked" → select this folder.
2) Open the popup. Test "Summarize".
3) If you have a valid Writer Origin Trial token for THIS extension ID, it is already added in <head>. Otherwise remove the meta tag or replace it.
4) If Writer still shows unavailable, your token may not match the extension ID or Chrome version.
